# S11 — 3 本に共通の小物を共有 crate と道具へ（`docs/plans/shared-crate-plan.md` §6 の S11 の行）

2026-09-22。worktree `rubevy_games-wt-s11`、ブランチ `s11`、先頭は main の `087fc06`。
`CARGO_TARGET_DIR` はこの worktree の `target`。4 項目:

1. 走行の終わり方を 1 か所に（`--shot` と判定が互いを打ち切れないように、両方向）
2. 正でない設定を読み込み時に断る口を `games_shell::Settings` へ
3. `web/build.sh` が `wasm-opt` を見つけられないとき黙って進まない
4. 確認の作法を `docs/verification/selftest-lines.md` に

## 0. 着手前の状態

| | |
|---|---|
| `cargo build --workspace --all-targets` | 警告 0 |
| `cargo test --workspace` | **110** 通過（46 + 25 + 11 + 18 + 6 + 4 doc）|
| Docker | 通る（`docker info` exit 0）|
| `wasm-opt` | PATH に無い。`~/.local/binaryen-version_132/bin/wasm-opt` にある（(3) の題材そのもの）|
| 機械 | `vmstat` の idle 91〜92%。隣の担当が別の `CARGO_TARGET_DIR` で cargo を回している。速さは測らない仕事なので、行の集合と絵で確かめる |

読んだもの: 計画書 §6 の S11・S9・S10 の行と §7、`factory-plan.md` §7 の 09-21 F1／F2／F3／F3a の該当行、
worklog の `-s9.md`（§1・§10）、`-s10.md`（§1・§5.6）、`-factory-F1.md`（§5.2）、`-factory-F3a.md`、
`docs/verification/selftest-lines.md` 全文、`tools/fixedlines.sh`、`crates/games-shell/src/{checks,settings,args,hud}.rs`、
3 本の `main.rs` の `take_shot` と selftest の終わり、`web/build.sh`、`.github/workflows/pages.yml`。

---

## 1. (1) 走行の終わり方を 1 か所に

### 何が置いてあったか — 半分ずつ 2 か所

走行が「ゲームであること」の上に頼まれることは 2 つある（判定と絵）。そのどちらも、**済んだ瞬間に自分で
`AppExit` を書いていた**ので、互いを打ち切れた。

* S9 は片方向を直した: `games_shell::checks::checks_end_the_run()` が `Args::from_env()` で `--shot` を見て、
  絵が頼まれていれば判定は終わらない。garden と sabibots がこれを使う。
* F1 と F2 はもう片方向を factory の中だけで直した: `take_shot` が `Option<Res<SelfTest>>` を読み、
  `!t.done` なら絵の後も走り続ける。

つまり**同じ 1 つの問いの半分ずつが 2 つの場所に、別々の形で**あった。その帰結が 09-21 S9（マージ）の
気づいた点で、PC の `--shot` つきの走行の最後の行が
`selftest: done — the garden keeps running (a page has nothing to exit to)` — ブラウザの理由を PC の走行が
言う嘘である。factory の `main.rs:1821` は `platform::CHECKS_EXIT_WHEN_DONE && shot.is_none()` で、
これも同じ文面を言っていた。

### 形 — `Args::from_env()` か Resource か

**Resource にした**（`games_shell::checks::Errands`）。理由 3 つ、どれも `argv` では届かないもの:

1. **半分は command line に無い。** 「絵はもう撮ったか」は走行の事実であって、走行が始まった語の事実ではない。
   `argv` に訊ける限り S9 が答えた半分しか答えられない。
2. **ページには command line が無い。** `Args::from_env()` は wasm で空を返すので、ブラウザでは
   `CHECKS_EXIT_WHEN_DONE` 以上のことを言えない。ブラウザはこの 3 本の揺れが 2 つとも出た場所である（S8、F3）。
3. 毎フレーム走る system が読む。`Args::from_env()` は訊かれるたびに command line 全部を `Vec<String>` に集める。

`Errands` は `Resource` であると同時に `Plugin` にした。ゲームが書くのは 1 行:

```rust
app.add_plugins(platform::Errands::of("the garden", selftest && headless.is_none(), shot.is_some()));
```

`impl Plugin for Errands` が自分を `insert_resource` し、`Last` に `end_the_run` を足す。
2 行（resource を入れる行と plugin を足す行）に割ると、片方を書き忘れた走行が「頼まれたことが無い走行」に
見えてしまう — 足し忘れが静かに通る形を作らないために 1 行にした。

### 「何も頼まれていない走行は終わらない」

`left()` は「まだ済んでいない errand の理由」を返し、`may_end()` は `left().is_none()` **かつ**
「何か 1 つは頼まれている」。この 2 つ目が無いと、**判定も絵も無い普通の遊びの走行が最初のフレームで終わる**。
単体テストの最後の塊がそれで、`Errands::of("the factory", false, false)` は `the_checks_are_done()` と
`the_picture_is_taken()` を両方呼んでも `may_end()` が false のままであることを確かめている。

### `done` の行 — 文面と、どの走行で出るか

`done` の行は `end_the_run` の 1 か所が書く。ゲームが渡すのは主語 1 語（`"the garden"` / `"the match"` /
`"the factory"`）だけで、文面は 1 つ:

```
selftest: done — {主語} keeps running ({理由})
```

理由は `left()` が返す 3 つのうちの 1 つで、訊く順は「この走行がまだやっていること」が先、
「台の事情」が後:

| 理由 | いつ |
|---|---|
| `the checks are not finished` | 判定がまだ（この理由で `done` の行は出ない — `done` は判定が終わった行だから）|
| `the picture is still to be taken` | **新**。PC の `--shot` つきの走行で、判定が絵より先に終わったとき |
| `a page has nothing to exit to` | ブラウザ（今までと 1 文字も変わらない）|

**9 通りの走行の行の集合は動かない**: ブラウザ 3 本の `done` の行は前と同じ文字列、ヘッドレスと窓（`--shot`
無し）は `done` の行そのものが無い（終わるから）。増えるのは `--shot` と判定を同時に使った走行だけで、
それは `selftest-lines.md` の 9 つの一覧のどれでもない。

**ただし 1 つ、前と違う形がある**: `--shot` の方が先に終わった走行（factory の既定 8 秒の絵と 13 秒の判定が
その形）では、判定が終わったフレームに残りの errand が無いので走行はそこで終わり、`done` の行は出ない。
前の factory は `shot.is_some()` を見ていたので、絵を撮った後でも `done` を言っていた。
「判定は済んだ、だが走行は続く」が `done` の行の意味なので、続かない走行が言わないのは正しい。
`selftest-lines.md` にそう書いた。

### `take_shot` は絵を撮るだけになった

3 本とも `MessageWriter<AppExit>` が `ResMut<Errands>` に替わった（引数の数は同じ — 箱庭の
`window_selftest` と Battle の `selftest` は Bevy の上限 16 個ちょうどなので、**足すのではなく入れ替える**
必要があった）。`take_shot` に残っている数（箱庭 2 秒、Battle と factory 1 秒）は
「観測者がファイルを書くのを待つ猶予」であって走行の長さではないので動かしていない
（出どころは相変わらず書いていない。§5 の気づいた点）。

factory の `watch_the_frames`（`?stress`）も `CHECKS_EXIT_WHEN_DONE` を見ていたので、同じ `errands.left()`
に替えた。stress の走行に `--shot` を付けたときだけ効く 1 行だが、直さなければ 3 か所目の同じ嘘になる。

### 確かめたこと

* 単体テスト 2 本（規則そのものと、system を回したときの終わり方）。S9 の `checks_end` の 1 本は
  役目が無くなったので置き換えた。テスト 110 → 112。
* ヘッドレス 3 本を回し、`tools/fixedlines.sh` で `selftest-lines.md` の一覧と diff（§4 に結果）。
* 窓とブラウザ、`--shot` つきの走行は §4。

---

## 2. (2) 正でない設定を読み込み時に断る口

### 拾った一覧（grep）

`.max(` / `.min(` / `.clamp(` が**設定の読みに掛かっている**所を全部拾った（`grep -rn '\.max(\|\.min(\|\.clamp(' --include=*.rs crates/ garden/src sabibots/src factory/src` から、
設定の値に掛かっていないもの — 描画の丸め、幾何、パネルの矩形 — を落としたもの）。
それと、**clamp が無くても 0 や負や文字を黙って受けていた設定**も同じ口に載せた（`headless_seconds` など）。

| # | どこ | 何を縛っていたか | S11 でどうしたか |
|---|---|---|---|
| 1 | `games-shell/checks.rs:186` `checks_instructions_a_frame` | `.max(1.0)` | **`positive`**。0 は「1 命令/フレーム」ではなく断り、測った既定 45,600 が立つ |
| 2 | `games-shell/hud.rs:99` `hud_bar_ticks` | `.max(0.0) as u64` | **`counted(0)`**。目盛り 0 本のバーは頼める、1.5 本は頼めない |
| 3 | `garden` `separate_passes` | `.max(0.0) as usize` | `counted(0)` |
| 4 | `garden` `light_shadow_cascades` | `.max(1.0) as u32` | `counted(1)` |
| 5 | `garden` `sky_sides` / `sky_rings` | `.max(3.0)` / `.max(2.0)` | `counted(3)` / `counted(2)`。**黙って持ち上げるのをやめた** — `sky_sides=1` と書いた店に三角形を描いて返すのは、その店が頼んだことではない |
| 6 | `garden` `edge_trees` | `.max(0.0) as usize` | `counted(0)` |
| 7 | `garden` `look_gait_blend_ms` | `.max(0.0) as u64` | `counted(0)` |
| 8 | `garden` `Furniture::count`（`start_plants` / `start_beetles` / `start_rabbits` / `start_trees` / `start_rocks`） | `.max(0.0) as usize` | `counted(0)` |
| 9 | `garden` `start_tries` | `.max(1.0) as usize` | `counted(1)`（横のコメントの理由そのまま: 0 回試すと `Vec2::ZERO` に置かれる）|
| 10 | `garden` `Budgets::count`（`script_budget` / `world_script_budget`） | `.max(0.0) as u64` | `counted(0)` |
| 11 | `garden` `headless_seconds` / `shot_seconds` | 縛り無し（0 も負も通った） | **`positive`** |
| 12 | `sabibots` `window_width` / `window_height` | 窓を開ける行で `.max(1.0)` | `counted(1)`。読む所で断るので、開ける行は `as u32` だけになった |
| 13 | `sabibots` `look_floor_pattern` | `.max(1.0) as i32` | `counted(1)` |
| 14 | `sabibots` `script_budget` | `.max(0.0) as u64` | `counted(0)` |
| 15 | `sabibots` `headless_seconds` / `shot_seconds` | 縛り無し | `positive` |
| 16 | `factory` `camera_half_height` | F1 の `positive`（この repo の原型） | `Settings::positive` に引っ越し、ゲーム側の 12 行を削除 |
| 17 | `factory` `window_width` / `window_height` | 窓を開ける行で `.max(1.0)` | `counted(1)` |
| 18 | `factory` `stress_items` / `stress_arms` | `.max(0.0) as usize` | `counted(0)`（`--stress` / `FACTORY_STRESS` は引数と摘みなので店の口ではない。`.max(0.0)` のまま）|
| 19 | `factory` `headless_seconds` / `shot_seconds` | 縛り無し | `positive` |
| 20 | `factory` `script_budget` | `.max(0.0) as u64` | `counted(0)` |

**縛ったまま残したもの**（理由が書いてあるので S11 は意味を変えない。`docs/numbers.md` §10 に一覧）:

| どこ | 何を | 理由 |
|---|---|---|
| `garden` `night` | `.clamp(light_dial_min, light_dial_max)` | ダイヤルの範囲。ダイヤルで選べない明るさを店から入れても、そのダイヤルは表示できない |
| `garden` `--eye` | `.clamp(eye_at.zoom_min, eye_at.zoom_max)` | 引数（店ではない）。カメラ自身の範囲 |
| `factory` `inserter_stagger` | `.max(0.0)` | **負の散らしと散らし無しは同じ走行**。黙って置き換えたものが無いので、店に言うことが無い（コードにそう書いた）|
| 3 本の `script_frame_time_ms` | `ms > 0.0` で `None` | rubevy の「時計を持たない」。0 以下は**意味のある指定**で、断ってはいけない |
| `sabibots` `handler_frames` / `garden` `egui_frames` | `.max(0.0) as u32` | 判定の摘み（環境変数と `?selftest&…`）であって店ではない |
| `rubevy-egui` `vm_regs_frames` / `vm_value_chars` / `code_rows` / `code_chars` | `.max(0.0)` / `.max(1.0)` | **口が届かない**: `rubevy-egui` は設計上 `Settings` を知らず（S4a）、`impl Fn(&str) -> Option<f32>` のクロージャを受け取る。直すには公開の署名を変えることになるので、S11 では触らず報告に出す |

### 形 — `&self` と `Mutex`、そして「後で言える」こと

口は `Settings` の 2 つ。既存の `number` に合わせた:

```rust
pub fn positive(&self, key: &str, default: f32) -> f32
pub fn counted(&self, key: &str, least: u64, default: u64) -> u64
```

**`counted` に `least` を足したのは計画書の例（`counted(key, default)`）と違う。** 理由は一覧の 4・5・9 で、
床が 0 のもの（木の本数）と 1 のもの（影のカスケード）と 3 のもの（空のドームの辺）が同じ数だけあるから。
床を呼ぶ側に置けば、**その床の理由は呼ぶ側の 1 行のコメントに書ける** — `Settings` の中に置くと
「なぜ 3 なのか」を言える場所が無くなる（「床は、どこから来たか言えない数であってはならない」）。
戻りは `u64` にした（`script_budget` が u32 に入らない）。

**`&self` にして `Mutex<Vec<String>>` で断りを溜める。** `&mut self` にすると、パネルに渡している
`impl Fn(&str) -> Option<f32>` のクロージャ（S5b-1）と、3 本の `read_from(&mut self, settings: &Settings)`
を全部可変にすることになる — 狭いことを言うために広く触る。ロックを取るのは起動時だけで、フレームの中では取らない。

**`number` そのものも直した**: 値があって数でないとき（`speed=fast`）は `None` を返すだけでなく断りを残す。
0 と負だけでなく「文字」も断れという確認の条件は、ここで満たされる。

### log に本当に出ることを実物で見る（F3a の教訓）

3 本とも設定は `main()` で読む。**`main()` の `warn!` は何も出ない** — `LogPlugin` は `DefaultPlugins` /
`MinimalPlugins` の中なので、`App` を建てる前には subscriber が無い（F3a がそれを書いて気づいた）。
だから断りは `Settings` が溜め、`games_shell::SettingsRefusalsPlugin` が `Startup` で 1 行ずつ `warn!` する。
3 本とも窓とヘッドレスの両方に足した（予算も家具もヘッドレスの走行の設定である）。
**本当に出ることは §4 の実走行で見た。**

---

## 3. (3) `web/build.sh` が `wasm-opt` を見つけられないとき

### 何が起きていたか

`wasm-opt` はこの機械では PATH に無く、`~/.local/binaryen-version_132/bin/` に展開してある。
`web/build.sh` の `command -v wasm-opt` は外れ、`else` の 1 行（stderr）を出して先へ進む。
その 1 行は長いビルドの真ん中に出て、**その後に出る大きさの行**（`garden/game_bg.wasm: N bytes …`）は
縮めていない大きさを「大きさ」として報告する（F1 の気づいた点）。

### 直し方と、止めない理由

1. PATH に無ければ `~/.local/binaryen-*/bin/wasm-opt` を探し、見つけたらそれを使って
   `wasm-opt: /home/…/bin/wasm-opt (not on PATH)` と言う（どの道を通ったかが見える）。
2. 見つからなければ今までどおり進み、**最後にもう一度はっきり言う** — 大きさの行の後、いちばん最後に、
   縮めていないゲームの名前と、どうすればよいかを 3 行（stderr）。

**止めない**と決めた。理由は「この道を通る走行はどれか」: 縮めていない module も動くし、
Playwright の確認の前の `web/build.sh garden` は**大きさを気にしない走行そのもの**である。
大きさが効くのは公開する走行で、それは CI であり、CI は binaryen を自分で展開して PATH に入れるので
この道を通らない（`.github/workflows/pages.yml` の `BINARYEN: "132"` の段）。
**CI の道は 1 文字も変えていない**: `command -v wasm-opt` が先に当たるので、探す側の行は CI では走らない。

`NOT_SHRUNK` は配列で、`all` のときにどのゲームが縮んでいないかを名前で言える。
確かめ: `bash -n`、PATH 無しで候補が見つかること（`found /home/kishima/.local/binaryen-version_132/bin/wasm-opt`）、
そして §4 の `web/build.sh all` + `web/build.sh factory` の実走行（**`export PATH=…` を前置きせずに**回して、
`wasm-opt:` の行が出て module が縮んでいること）。
