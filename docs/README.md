# rubevy_games documents

Laid out as every repository of the organization is
([`.github/CONTRIBUTING.md`](https://github.com/sabiruby/.github/blob/main/CONTRIBUTING.md));
small enough to stay flat; plans have their own directory.

| file | what it is |
|---|---|
| [sabiruby-battle.md](sabiruby-battle.md) | SabiRuby Battle: the two kinds of script, the boundary (`Rubevy.ask`), the tank model and the randomness, the DSL, handlers, the editor, the VM panel, restarting, the in-game guide (`H`), what is not done yet |
| [garden.md](garden.md) | Garden: the second sample — a 3D world whose creatures read and write their own ECS components from Ruby by name. What exists after G0, G0a, G1, G2, G3, G4 and G5: the component table, the rules, the CC0 models and their animations, the DSL the two creatures are written in, the wheel a handler takes, the four questions, the `Genome` a Rust struct and a Ruby class both are (with the line count of the same class written by hand), breeding, the save file — the world and every creature's own `@memory` as JSON, read out of the VM without asking the script — and the window: an editor whose unit is the *species*, the VM panel, and a HUD whose "instructions per decision" is defined and measured (it stands where "frames per decision" did, which stopped meaning anything when a component read stopped costing a frame); and the browser build, where the save is a `localStorage` string and therefore carries a version number; and G6's play: how dark the night is, a wheel that reads its unit, panning, and the in-game guide with the font that had to be bundled for it; and G6b's: a night raised again to a measured 44 of 255 with a dial in the panel for the author to settle it, and a guide that shows one language at a time; and G8's: the horizon — a ground that runs on past the wall, fog coloured with the sky's rim, an 84-triangle dome whose gradient is its vertex colours — and a colour per species, the same number on the model and in the HUD; and G9's: `P` stopping the world rather than only the Ruby, and a VM panel that opens closed and, when it opens, says in a sentence what each behaviour is waiting for and on which line of its own file; and W1–W3's: **the rules of the world in Ruby too** — `ruby/world.rb`, one pass of `each_frame` a frame in a second VM, holding the grass, hunger, eating, pairing, the child and the seasons, with the mechanics, the contact detection, the drawing and the one spatial question (`garden.within`, answered by Rust inside the tick) left where they were; the two VMs' ticks ordered so that a meter written this frame is read this frame; the budget the second VM gets and the measurement at the garden's own caps it was chosen from; and `F3`, which opens the rules in the same editor, with the same three buttons, while the garden goes on running |
| [web.md](web.md) | the browser build: one Pages site and two games (and the address SabiRuby Battle moved from), what differs from the PC build and where, the two wasm modules, the keys a browser wants for itself, sizes with and without `wasm-opt` (and what the bundled Japanese font cost), and what was driven in a headless browser |
| [wsl-gpu.md](wsl-gpu.md) | running the games on WSL2 without a GPU driver (Docker, WSLg, lavapipe) |
| [plans/showpieces-plan.md](plans/showpieces-plan.md) | (Japanese) to do: a handler on a hit, the VM inspector in the game window, the DSL over `Entity#[]` and `Proxy` |
| [plans/garden-world-plan.md](plans/garden-world-plan.md) | (Japanese, 2nd edition) the garden's world rules in Ruby: `world.rb` in a second VM whose `each_frame` runs the rules themselves — growth, hunger, eating, mating, starving — on synchronous reads, with the spatial question (`garden.within`) answered by Rust inside the tick; mechanics, contact detection and delivery stay in Rust; W1–W3, status inside |
| [plans/garden-plan.md](plans/garden-plan.md) | (Japanese) the second sample, Garden: creatures whose behaviours read and write ECS components by name (`Entity#[]`), a `Genome` class made from a Rust struct by the macros, save/load through serde and `JSON`, events and a `Proxy` for the two rule questions |
| [worklog/2026-09-18-selftest-fixes.md](worklog/2026-09-18-selftest-fixes.md) | (Japanese) the three flaky checks mended, one commit each, with no threshold moved and one new number: **the wall is an immovable thing in the pushing too** — what it refuses one of a pair is handed to the other, so the write-back has nothing left to clamp and the overlap the clamp used to make is gone by construction (the survey's own instrument, two runs back to back: 17 pairs under 0.99 of the radii before, none after, and 156 pushes the wall refused something to, all settling at 0.999 or better); **a probe a rabbit walked into has measured nothing** — a third verdict, `selftest: n/a`, which is neither ok nor FAIL, in the shape the sixth check already had for a touch it could not read; and **a creature born into the night is told that it is night**, two frames after its body exists, which is where its script first subscribes and is measured rather than chosen — with the check written as a check (newborns aside, on the `NEWBORN_GRACE` that was already there). Shown with creatures born one frame apart across nightfall: before, the four born from one frame before the publish onwards walked at `CRUISE` all night; after, all eight asleep. Then forty ninety-second runs, eight at a time: **no FAIL**, one `n/a` where a rabbit really did walk into the probe, closest pair 0.981 to 1.000 (median 0.998) |
| [worklog/2026-09-17-selftest-flakes.md](worklog/2026-09-17-selftest-flakes.md) | (Japanese) a survey, no code changed: the three checks of the garden that fail now and then, measured over 98 ninety-second runs (4/98, 4/98, 0/98) and then taken apart with throwaway instruments — the fourth is **the wall**, not the four separation passes: the passes push the pair exactly apart and the write-back's `clamp` pulls the one that went through the wall back into the other (23 events out of 23, and not one where the passes themselves fell short); the fifth is **a rabbit walking into the probe's corner**, which is only kept clear at spawn time — `on(:touched)` turns the beetle away at full speed and the distance of that frame (1.8–2.3) is what the check prints, and "it started closer than it ever got again" is why there are no intermediate values; the seventh is **two frames of deafness**, shown by hand with births placed one frame apart around nightfall: a creature born in the frame of a `publish`, or the frame before it, is already counted among its addressees and has not subscribed yet — and the reason it almost never fires is that the twelfth check's thaw pins a burst of births five frames clear of the night. With, for each one, two or three ways out marked by what they move (a threshold, the sentence, or the behaviour), and the note that "it began when the order changed" does not survive a Fisher test (p = 0.24) |
| [worklog/2026-09-17-garden-world.md](worklog/2026-09-17-garden-world.md) | (Japanese) W1 of the garden's world rules in Ruby: a second VM (`struct World`) whose one script runs the rules themselves every frame — the grass growing and sprouting, hunger, eating and starving moved out of five Rust systems into `ruby/world.rb`, with the same numbers — how `each_frame` is made to be exactly one frame (`Rubevy.ask("frame")`, and every other question on the no-frame road), the spatial question `garden.within` answered by a closure inside the tick, what the two VMs' ticks had to be ordered against, the three checks that were watching a Rust system and now watch what Ruby did to the world, and the measurement the world VM's budget was then chosen from; and **W2**: the pairing and the child moved out of `court` and `hatch` — the cooldown kept in a component the rules write (and why an `Option<Entity>` cannot be one of its fields), a newborn found by "age is zero and somebody asked for me", the cost of a child charged a pass later so that two writes to one meter cannot race; `tell` and `every` (a timer task that has to be told its rules were replaced), the dry and the wet season, `"ate"` back in the save's `@memory`, and the thirteenth check reading a creature's memory out of the VM; and **W3**: `F3` and the editor's third file — why `Brains` grew a slot rather than a sibling, the one file-watcher test that was matching `world_prelude.rb` as if it were a creature's, the HUD's line for what a pass of the rules costs, the eleven window checks driven by real keys (and why `Sky::day_length` is the thing they watch), and the budget measured again at the caps: the straight-line law W1 read it off stopped describing W2's rules, so the number stayed and its reason changed |
| [worklog/2026-09-17-sync-reads.md](worklog/2026-09-17-sync-reads.md) | (Japanese) taking rubevy's synchronous component reads into the two games: the measurement that says the update alone breaks nothing (both games' checks pass, before a line was changed) and that what it *did* break is the number the HUD was quoting — "frames per decision", which counted a read by the gap it left and now counts three or four gaps that are not reads at all; the probe that shows the garden's rule chain is split around the tick and split differently every frame; why `.before(RubevySet::Tick)` was measured and then **left out** (it makes the sixth check fail in half the runs, with the beetle fleeing at full speed along a course 16° from the one it was on); "instructions per decision" put in the old number's place, defined off the one gap the scripts still make; and the VM panel's sixth reason for waiting, deleted with the case where it is still reachable written down |
| [worklog/2026-09-17-garden-world-survey.md](worklog/2026-09-17-garden-world-survey.md) | (Japanese) a survey, no code changed: what it would take to move the garden's **rules** (day and night, the grass, hunger, eating, courting and birth, bumping) out of Rust and into a Ruby `world.rb` the editor can swap like a creature's file — every rule system with what it reads, writes and publishes; every number it holds and whether it is a `const` or a setting (all but the night dial are consts); the events, payloads, questions and components a creature sees; where the scripts live and how `Ctrl+Enter` replaces a whole species (and what a browser does instead); what the ten checks measure; what the save does and does not carry; the loop counts per frame and how `P` stops the world but not the answering; and what rubevy offers today — including that **a script cannot `publish`** and that a second VM's `publish` does not cross to the first |
| [worklog/2026-09-17-garden-guide-rewrite.md](worklog/2026-09-17-garden-guide-rewrite.md) | (Japanese) the garden's guide rewritten by the author as plain how-to-play — the three mechanism points it dropped, the English brought into line, the 18 characters the subset font did not have and the one-liner that finds them, and why `cargo` was kept off this checkout while rubevy's multi-VM work was building it |
| [worklog/2026-09-17-garden-G7.md](worklog/2026-09-17-garden-G7.md) | (Japanese) renaming the demo's own vocabulary out of it: `reflex` → `on` (and the machinery under it → `handler`), brain / mind / 「頭脳」 → behaviour / 「行動アルゴリズム」 — where the three lines between "a name on the screen", "a name in the source" and "an identifier" were drawn, the six places a word-for-word substitution got wrong (a worklog's file name, an article, a comment rule four characters too long), the guide paragraph that now says all three things an `on` block does, a font subset that got *smaller* for the first time, and the three pictures that were out of date because the editor panel in them was showing the old keyword |
| [worklog/2026-09-17-garden-G9.md](worklog/2026-09-17-garden-G9.md) | (Japanese) `P` stopping the world in both games — the run condition that was already there for loading a save, the one line that holds the garden's clock (and why that is also why no `"night"` can be lost over a pause), and the match clock Battle had to grow so that a rule with `every:` does not come due five times at once on the frame the pause ends — and the VM panel turned round to read top down: how "why it is waiting" is worked out from the frames alone (`Task::Queue#pop` is Ruby and `sleep` is a native, so one leaves a frame and the other does not), the one case it cannot tell apart and says so, the throwaway instrument whose output became six unit tests, and the three pictures that were quietly out of date because the panel in them no longer exists in that form |
| [worklog/2026-09-17-garden-G8.md](worklog/2026-09-17-garden-G8.md) | (Japanese) the look, after the author's third play: why the first horizon showed no sky at all — the default camera looks down at 49° and the true horizon is 26° above the top of the frame, which leaves fifteen units of depth to put fog in — the sky and the far ground riding with the camera so both can be small, a gradient that is 49 vertex colours because `StandardMaterial` *assigns* the vertex colour over `base_color` rather than multiplying, trees past the wall that carry no component so `garden.nearest` still means the garden, and the tint that had to throw the texture away because Kenney's `colormap.png` turns out to be a palette of flat squares and not a shading map; and how three pictures of one garden wearing three different beetles were taken when the world is seeded from the clock and nothing can be told to hold still |
| [worklog/2026-09-17-garden-G6b.md](worklog/2026-09-17-garden-G6b.md) | (Japanese) the author's second browser play: a night raised a second time — this one against a figure (40–50 of 255 for the ground at midnight) — and what measuring it said, that the ambient light is worth almost nothing to that number and that what separates day from night is not the moon's strength but its height, since it is aimed at `-up` and only stands overhead at midnight; the dial that exists because a brightness is a fact about somebody else's monitor, and the settings store behind it, which is two functions the games already had; and the guide showing one language with a button to switch, where egui takes a window's title as its id and the switch therefore threw the window back to the middle of the screen |
| [worklog/2026-09-17-garden-G6.md](worklog/2026-09-17-garden-G6.md) | (Japanese) what the author's browser play found and what it took to fix: a night whose two numbers turn out to do different jobs (the moon draws the edges, the ambient decides whether a beetle under a tree exists) and the `--at midnight` that made comparing them cheap; a wheel with two steps in it, because `MouseWheel::unit` was never read — a browser sends 100 pixels to the notch and a PC sends one line — and why the fix is a ratio rather than a subtraction; the `CameraLog` that exists because a camera inside a wasm canvas can only be read through the console, and what that measured; and a Japanese font cut down to the 327 characters the guide uses, which means the text and the font have to be cut together |
| [worklog/2026-09-17-garden-vm-0.5.1.md](worklog/2026-09-17-garden-vm-0.5.1.md) | (Japanese) taking sabiruby 0.5.1 into the Garden: the restart queue G4 needed deleted and the replaced-at-once table taken again (no cliff at ten), a committed `GARDEN_RELOAD_AT` so the checks can press F9 with no keyboard — and the measurement that says G5's lost memories were the same VM bug and not `restore_memory`'s timing (the restore takes 1.48 s on 0.5.0 and 6 ms on 0.5.1, and `RESTORE_PATIENCE` is five seconds, which is why only a browser ever saw it) — and the page that stopped answering the keyboard after `?selftest`, which was the checks writing `AppExit` where there is nothing to exit to |
| [worklog/2026-09-17-battle-followups.md](worklog/2026-09-17-battle-followups.md) | (Japanese) taking rubevy `fa37eaa` and sabiruby 0.5.0 into the battle: the handler priority turned the other way round (and the workaround it made unnecessary), `answer_requests` in `RubevySet::Answer` and the round trip measured again, the paused clock, and the VM panel's `ctx` read from the VM rather than out of a string |
| [worklog/2026-09-17-garden-G5.md](worklog/2026-09-17-garden-G5.md) | (Japanese) the Garden in a browser: two games on one Pages site and the address Battle moved from; a save that carries a version number, read on its own before the rest, and the tenth check put through `--load` itself; the two tools the machine was said not to have and did; `wasm-opt -Os` making the file smaller and the download bigger; and the three things driving the page in headless Chromium turned up — a restart that leaves every new task made but never run, a page with the checks on that stops answering the mouse, and a compiler bridge that cannot pass a file's name |
| [worklog/2026-09-17-garden-G4.md](worklog/2026-09-17-garden-G4.md) | (Japanese) the Garden's window, and the two real bugs behind the sixth check's flakiness: a `@course` written by an `act` that never went out, so a handler swerved to the wrong side; and a settling clock that the check reset inside its own exclusions, so 41 of 211 counted touches were answering an older message. With the hypothesis that was measured and thrown away, how "frames per decision" is defined so that it can be wrong, and what a second game cost the shared editor (four fields) |
| [worklog/2026-09-17-garden-G3.md](worklog/2026-09-17-garden-G3.md) | (Japanese) the Garden's save file: why a task's `@ivar` is every task's, the one line of prelude that lets the host find a creature's object, why a loaded garden stands still while its behaviours start, the spawn Hash read by serde in 7 lines where 72 were, and the unit in the last place that serde_json's parser loses without `float_roundtrip` |
| [worklog/2026-09-17-garden-G2.md](worklog/2026-09-17-garden-G2.md) | (Japanese) the Garden's `Genome`: a Rust struct made into a Ruby class by `#[derive(RubyClass)]` and `#[ruby_methods]`, counted against the same class written by hand; why a method that takes another object of its own class takes a raw `Value`; why the dice are the VM's; and why "two creatures that touch" was a rule that almost never fired |
| [worklog/2026-09-17-garden-G0a-G1.md](worklog/2026-09-17-garden-G0a-G1.md) | (Japanese) the Garden's models and its behaviours: a `bevy_gltf` that cargo swore did not exist, what `Scene` is called in bevy 0.19, why the animal pack had no rabbit, and the four things that had to be fixed before "a startled beetle turns" was true — ending in a wheel that one task holds |
| [worklog/2026-09-17-garden-G0.md](worklog/2026-09-17-garden-G0.md) | (Japanese) building the Garden's world: what `bevy_pbr` dragged in behind it, how a 3D scene runs headless with no renderer, the placeholder behaviour G1 deletes, and how the starvation check was made to happen on purpose rather than by luck |
| [worklog/2026-09-16-showpieces-d2-d3.md](worklog/2026-09-16-showpieces-d2-d3.md) | (Japanese) what rubevy took off the game's hands (the entity a child task carries, the end of a subscription), building the VM panel — the one entry point the VM has not got and how the `ctx` was got out of it anyway — and the study of writing the robots over `Entity#[]` and `Proxy` |
| [worklog/2026-09-16-reflex.md](worklog/2026-09-16-reflex.md) | (Japanese) building `on` (called `reflex` then): what broke when one robot used two tasks — the entity a `Task.new` task has not got, the nested run loop a task cannot park across, and why last-writer-wins makes the quicker task lose |
