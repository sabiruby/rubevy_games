[package]
name = "sabibots"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "SabiRuby Battle — robots whose brains are Ruby, running as tasks in one VM"

[dependencies]
bevy.workspace = true
rubevy = { workspace = true, features = ["ruby-source"] }
sabiruby.workspace = true
sabiruby-compiler = { version = "0.2.1", features = ["host"] }
rubevy-arena.workspace = true
